{
  "name": "voice-bridge",
  "version": "1.0.0",
  "description": "Voice Bridge -- WebRTC video calls with live, translated captions (Socket.io signaling server)",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server",
    "build": "npm --prefix frontend run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ankit Pokhrel",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
